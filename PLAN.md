# 🎯 Co-Bingo 구현 계획서

> QR 코드로 휴대폰을 연결하여 플레이하는 실시간 멀티플레이어 빙고 게임

---

## 📋 요구사항 요약

| 항목 | 내용 |
|------|------|
| **인원** | 최소 2명 ~ 최대 12명 |
| **연결** | QR 코드 스캔으로 방 참가 |
| **재접속** | 연결 끊김 후 재접속 시 게임 상태 복원 |
| **배포** | GitHub → Vercel (인터넷 접속) |

---

## 🏗️ 아키텍처

### 기술 스택

| 구분 | 기술 | 이유 |
|------|------|------|
| **프론트엔드** | HTML + CSS + Vanilla JS (SPA) | 가볍고 빠른 로드, 빌드 불필요 |
| **실시간 동기화** | Firebase Realtime Database | 무료 티어, 자동 재연결, 실시간 리스너 |
| **QR 코드** | qrcode.js (CDN) | 클라이언트에서 QR 생성 |
| **배포** | Vercel (정적 사이트) | 무료, 빠른 CDN, GitHub 자동 연동 |

### 통신 구조

```
[폰 A] ←──→ [Firebase RTDB] ←──→ [폰 B]
   ↑              ↑                   ↑
   └── Vercel에서 정적 파일 서빙 ──────┘
```

- **Firebase RTDB** = 중앙 상태 저장소 + 실시간 동기화
- **Vercel** = 정적 파일(HTML/CSS/JS)만 서빙
- 별도 백엔드 서버 불필요 (서버리스)

### 왜 Firebase인가?

- Vercel은 서버리스 → 전통적인 WebSocket 서버 불가
- Firebase SDK가 네트워크 끊김/복구 자동 처리
- DB에 게임 상태 영구 저장 → 재접속 시 복원
- 무료 티어: 1GB 저장, 10GB/월 전송 (빙고 게임에 충분)

---

## 🎮 게임 규칙 (한국식 숫자 빙고)

1. 모든 플레이어가 동일한 숫자 세트(예: 1~25)를 **5×5 보드에 자유롭게 배치**
2. 플레이어가 **돌아가며** 숫자를 하나씩 호출
3. 호출된 숫자는 **모든 플레이어의 보드**에서 자동 마킹
4. 가로/세로/대각선 한 줄이 완성되면 **1빙고**
5. 설정된 빙고 수(기본 5줄)를 먼저 달성하면 **승리!**

---

## 📁 프로젝트 구조

```
co-bingo/
├── index.html              # 메인 SPA (Single Page Application)
├── css/
│   └── style.css           # 전체 스타일 (모바일 최적화)
├── js/
│   ├── app.js              # 앱 라우터 & 메인 컨트롤러
│   ├── firebase-config.js  # Firebase 설정
│   ├── db.js               # Firebase RTDB 래퍼 함수
│   ├── room.js             # 방 생성/참가/관리
│   ├── game.js             # 빙고 게임 로직
│   ├── ui.js               # UI 렌더링 & 컴포넌트
│   ├── qr.js               # QR 코드 생성
│   └── audio.js            # 효과음 (Web Audio API)
├── assets/
│   └── favicon.svg         # 파비콘
├── vercel.json             # Vercel 라우팅 설정
├── package.json            # 프로젝트 메타데이터
├── .gitignore
├── PLAN.md                 # 이 파일
└── README.md               # 설정 & 실행 가이드
```

---

## 📊 Firebase 데이터 구조

```json
{
  "rooms": {
    "ABCD1234": {
      "id": "ABCD1234",
      "host": "player_abc123",
      "status": "waiting | setup | playing | finished",
      "settings": {
        "boardSize": 5,
        "winCondition": 5,
        "maxPlayers": 12
      },
      "players": {
        "player_abc123": {
          "name": "민수",
          "order": 0,
          "board": [[3,17,24,1,8], [12,5,21,14,19], ...],
          "connected": true,
          "bingoLines": 0,
          "lastSeen": 1708500000000
        },
        "player_def456": { "..." }
      },
      "calledNumbers": [15, 3, 22, 7],
      "currentTurnIndex": 2,
      "turnOrder": ["player_abc123", "player_def456"],
      "winner": null,
      "createdAt": 1708500000000
    }
  }
}
```

---

## 🖥️ 화면 설계 (SPA - Hash 라우팅)

### 1. 홈 화면 (`#/`)
- 앱 로고 & 타이틀 **"Co-Bingo"**
- **"방 만들기"** 버튼 (큰 프라이머리 버튼)
- **"방 코드 입력"** 필드 + 참가 버튼
- 재접속 배너: *"이전 게임이 있습니다. 돌아가시겠습니까?"*

### 2. 로비 화면 (`#/lobby/{roomId}`)
- **방장 전용**:
  - QR 코드 (방 URL 인코딩)
  - 방 코드 텍스트 (복사 가능)
  - 게임 설정 (보드 크기, 승리 조건)
  - "게임 시작" 버튼 (2명 이상일 때 활성화)
- **공통**:
  - 참가자 목록 (실시간 업데이트, 연결 상태 표시)
  - 닉네임 표시

### 3. 보드 설정 화면 (`#/setup/{roomId}`)
- 빈 5×5 그리드
- 숫자 풀 (1~25) 탭하여 순서대로 배치
- **"랜덤 배치"** 버튼
- **"준비 완료"** 버튼
- 다른 플레이어 준비 상태 표시

### 4. 게임 화면 (`#/game/{roomId}`)
- **상단**: 호출된 숫자 히스토리 (가로 스크롤)
- **중앙**: 5×5 빙고 보드 (마킹된 셀 하이라이트)
- **하단**:
  - 현재 턴 표시 ("당신의 차례!" 또는 "○○님 차례")
  - 빙고 라인 카운트
  - 숫자 선택 패드 (자기 턴일 때만 활성화)
- **사이드**: 플레이어별 빙고 수 순위

### 5. 결과 화면 (`#/result/{roomId}`)
- 승리자 표시 + 축하 애니메이션 (confetti 🎊)
- 전체 순위표
- "다시 하기" / "홈으로" 버튼

---

## 🔄 재접속 로직

### localStorage 세션 정보

```json
{
  "co-bingo-session": {
    "roomId": "ABCD1234",
    "playerId": "player_abc123",
    "playerName": "민수"
  }
}
```

### 재접속 흐름

1. 앱 로드 시 `localStorage`에서 세션 확인
2. 세션 존재 → Firebase에서 해당 방 상태 조회
3. 방 존재 && 게임 진행중 → "이전 게임으로 돌아가기" 토스트 표시
4. 사용자 승인 → 자동으로 해당 화면(로비/게임)으로 이동
5. `connected: true` 업데이트, `lastSeen` 갱신
6. Firebase `onDisconnect` 핸들러: 자동으로 `connected: false` 설정

---

## ⚙️ 게임 설정 옵션

| 옵션 | 기본값 | 범위 | 설명 |
|------|--------|------|------|
| 보드 크기 | 5×5 | 3×3, 4×4, 5×5 | 빙고판 크기 |
| 승리 조건 | 5 빙고 | 1~12 | 달성해야 할 줄 수 |
| 최대 인원 | 12명 | 2~12 | 방 최대 참가 인원 |

---

## 📦 개발 단계

### Step 1: 프로젝트 초기화 ⏱️ ~10분
- 프로젝트 폴더 구조 생성
- `package.json`, `vercel.json`, `.gitignore` 생성
- `README.md` Firebase 설정 가이드 작성

### Step 2: UI 기본 구조 ⏱️ ~40분
- `index.html` SPA 셸 구현
- `style.css` 디자인 시스템 (CSS 변수, 모바일 퍼스트)
- `app.js` 해시 라우터 구현
- `ui.js` 화면 렌더링 함수

### Step 3: Firebase 연동 ⏱️ ~30분
- `firebase-config.js` 설정
- `db.js` CRUD 래퍼 함수
- 실시간 리스너 설정
- `onDisconnect` 핸들러

### Step 4: 방 시스템 ⏱️ ~30분
- 방 생성 (고유 ID 생성)
- QR 코드 생성 (방 URL 인코딩)
- 방 참가 (코드 입력 / URL 직접 접속)
- 로비 UI (참가자 목록, 설정)

### Step 5: 보드 설정 ⏱️ ~40분
- 보드 UI 구현
- 숫자 배치 (탭 방식)
- 랜덤 배치 기능
- 준비 상태 동기화

### Step 6: 게임 로직 ⏱️ ~40분
- 턴 시스템 구현
- 숫자 호출 & 전체 브로드캐스트
- 자동 마킹 로직
- 빙고 라인 판정 알고리즘 (가로/세로/대각선)
- 승리 판정

### Step 7: 재접속 & 안정성 ⏱️ ~20분
- `localStorage` 세션 관리
- 재접속 UI 플로우
- `onDisconnect` / `onReconnect` 처리
- 에러 핸들링

### Step 8: UI 폴리싱 ⏱️ ~30분
- 마킹 애니메이션 (펄스 효과)
- 빙고 달성 이펙트
- 승리 축하 (confetti)
- 효과음 (Web Audio API)
- 다크 모드 + 글래스모피즘

### Step 9: 배포 ⏱️ ~20분
- GitHub 저장소 생성 & 푸시
- Vercel 프로젝트 연결
- 실제 휴대폰 멀티 디바이스 테스트

---

## 💡 개발 팁 & 도구 활용

### AI 도구별 역할 분담

| 작업 | 추천 도구 | 비고 |
|------|-----------|------|
| **전체 계획 & UI 설계** | **Claude** (Gemini UI) | 아키텍처 설계, 화면 설계, UX 플로우, 프로젝트 관리 |
| **코드 구현** | **Codex** | HTML/CSS/JS 코딩, Firebase 연동, 게임 로직 구현 |
| **SVG 아이콘/에셋 생성** | **Gemini 3.1** | SVG 코드 생성 품질이 뛰어남. 파비콘, 로고, UI 아이콘 등 |
| **디자인 이미지 생성** | **Z-Image Turbo** (로컬 ComfyUI) | Claude가 ComfyUI API로 배치 큐잉하여 이미지 생성 후 가져옴 |

### SVG 활용 가이드
- **파비콘** (`assets/favicon.svg`): Gemini 3.1로 빙고 테마 SVG 생성
- **로고**: Co-Bingo 텍스트 로고 SVG
- **UI 아이콘**: 설정, 복사, QR, 왕관(승자) 등의 인라인 SVG
- SVG는 CDN이나 외부 라이브러리 없이 HTML에 인라인으로 삽입 가능 → 로드 속도 최적화

### 이미지 생성 환경 (Z-Image Turbo)

| 항목 | 값 |
|------|-----|
| **ComfyUI 경로** | `C:\Users\mintk\Documents\ComfyUI` |
| **ComfyUI API** | `http://127.0.0.1:8188` |
| **워크플로우** | `user/default/workflows/z_image_turbo_4070super.json` |
| **출력 경로** | `C:\Users\mintk\Documents\ComfyUI\output\` |
| **GPU** | RTX 4070 Super (12GB VRAM) |

**모델 구성:**
- Text Encoder: `qwen_3_4b_fp8_mixed.safetensors` (5.25GB)
- Diffusion: `z_image_turbo_nvfp4.safetensors` (4.51GB)
- VAE: `ae.safetensors` (0.31GB)

**생성 설정:**
- Steps: 4 (Turbo), CFG: 1.0
- Sampler: `res_multistep`, Scheduler: `simple`
- Resolution: 1024×1024
- Total VRAM: ~10GB

**배치 생성 방법:**
1. ComfyUI Desktop이 실행 중인지 확인 (`http://127.0.0.1:8188`)
2. Claude가 워크플로우 JSON의 프롬프트를 수정하여 ComfyUI API에 POST
3. 생성된 이미지를 `output/` 폴더에서 프로젝트 `assets/`로 복사
4. 필요한 이미지: 배경 패턴, 축하 이펙트 소스 등

---

## 🔧 Firebase 프로젝트 설정 (완료 ✅)

> **설정 일시**: 2026-02-21 10:20 KST
> **계정**: mintring125@gmail.com

### 완료된 설정 내역

| 항목 | 값 |
|------|-----|
| **프로젝트 ID** | `co-bingo-game` |
| **프로젝트 콘솔** | https://console.firebase.google.com/project/co-bingo-game |
| **웹 앱 ID** | `1:124442295475:web:851e2fce5f7d782b3604c6` |
| **RTDB 위치** | `asia-southeast1` (싱가포르) |
| **RTDB URL** | `https://co-bingo-game-default-rtdb.asia-southeast1.firebasedatabase.app` |
| **보안 규칙** | 테스트 모드 (30일 후 만료 - 이후 갱신 필요) |
| **요금제** | Spark (무료) |

### Firebase SDK 구성 값 (`js/firebase-config.js`에 사용)

```javascript
const firebaseConfig = {
  apiKey: "AIzaSyBMMY-wjTyBNLmyO2_4WCuO9W2kQAwqJgY",
  authDomain: "co-bingo-game.firebaseapp.com",
  databaseURL: "https://co-bingo-game-default-rtdb.asia-southeast1.firebasedatabase.app",
  projectId: "co-bingo-game",
  messagingSenderId: "124442295475",
  appId: "1:124442295475:web:851e2fce5f7d782b3604c6"
};
```

### Firebase CLI 명령어 참고

```bash
# 로그인 상태 확인
firebase login:list

# 프로젝트 목록
firebase projects:list

# SDK 설정값 확인
firebase apps:sdkconfig WEB "1:124442295475:web:851e2fce5f7d782b3604c6" --project co-bingo-game

# 보안 규칙 배포 (database.rules.json 파일 필요)
firebase deploy --only database --project co-bingo-game
```

---

## 🚀 배포 프로세스

### GitHub

```bash
git init
git add .
git commit -m "initial commit"
git remote add origin https://github.com/{username}/co-bingo.git
git push -u origin main
```

### Vercel

1. https://vercel.com 접속 → GitHub 로그인
2. **"Import Project"** → `co-bingo` 저장소 선택
3. Framework Preset: **"Other"**
4. Root Directory: `.` (기본값)
5. Build Command: 없음 (정적 사이트)
6. Output Directory: `.` (기본값)
7. **"Deploy"** 클릭

### 결과
- URL: `https://co-bingo.vercel.app` (또는 유사)
- 자동 HTTPS
- GitHub push 시 자동 재배포
